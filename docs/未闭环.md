# 未闭环（复盘）

| 项 | 内容 |
|---|---|
| 版本 | **v0.2** |
| 日期 | 2026-09-09 |
| 状态 | **额度暂停前快照** · 供下次续作对照 |
| 依据 | 代码核对 + `收尾计划`；驾驶舱见 `智能驾驶舱-信息架构.md` |

---

## 一句话

**机构健管主链已通**：看红 → 催办/提醒 → 随访/方案/报告 → 站内信召回；W4 观测与 MFA 已落地；**驾驶舱 P0 壳已上**。  
**还没闭环的**，主要是 C 端助手、患者合并前端、驾驶舱 P1+、外部触达通道（暂缓）。

---

## 1. 已闭环（不必再排）

| 域 | 能力 |
|---|---|
| 触达（站内） | IN_APP：报告/方案发布、日待办 `DAILY_HEALTH_TODO`、B 一键 `STAFF_NUDGE`；C 列表/角标/已读 |
| 看板 → 工作台 | 一键开随访 / `PLAN_NUDGE`；补打卡条件自闭 `PLAN_NUDGE` |
| 管理报告 | 周/月/季；AI 点评草稿；PDF（前端打印导出）；三月报 → `PLAN_CREATE` |
| 随访 | B 创建办结；C 只读；三类专属表单；排期 Job；入组基线写体征；C 申请回访 |
| 观测 W4 | 用药 BID/TID 按次；`adherence_daily_snapshot` + 机构趋势图；检验/检查 OCR **录入预填**（人审后落库） |
| 安全 | B/Ops MFA（TOTP）：登录动态码 + 安全设置绑定/启用/关闭 |
| SaaS 治理（配置层） | Ops 租户治理：配额、feature flag、白标配置；建档/员工/AI 等有服务端拦截 |
| 医生（基础） | 角色可授；健管组主责医生；方案医生签署 + `PLAN_REVIEW`（受 flag 控制） |

---

## 2. 明确暂缓（缺外部账号 / 资质）

| 项 | 说明 |
|---|---|
| SMS / 小程序订阅 / 公众号 / 企微 | App 外主动触达；无服务商账号前不做 |
| 短信 OTP 激活 | 依赖 SMS |
| 微信小程序形态 | 现网仍是 Web/H5 |
| 真实支付计费 | 仅做配额与白标配置层，不对接支付渠道 |

---

## 3. 仍未闭环 / 半开（下次优先）

### P0 级缺口

| # | 项 | 现状 | 建议下一刀 |
|---|---|---|---|
| A | **C `/discover` 助手** | 仍闲聊，未接档案/方案/用药/报告/随访工具 | `收尾计划` W5：PatientAgentGateway + 工具，对齐 Staff 侧模式 |
| B | **患者合并 S2 UI** | **后端已有** `POST /api/b/v1/patients/merge`（仅租户管理员）+ `PatientMergeService`；**前端无入口** | 租户管理或患者列表：选源/目标 → 确认 → 展示迁移摘要 |
| C | **智能驾驶舱** | **P0 壳已落地**：聚合 API + 三栏页（左优先/中对话简报/右快照）；默认机构首页。P1 动作卡、草稿箱聚合、消息持久化深化未做 | 见 `智能驾驶舱-信息架构.md` P1–P3 |

### P1 / 边界与尾巴

| # | 项 | 说明 |
|---|---|---|
| D | 随访排期可配 | 现硬编码有方案 30 天 / 无方案 90 天；未按病种或机构配置 |
| E | 对话侧检查 OCR | 录入页预填已通；Care Copilot 里 `OcrExamSkill` 仍占位跳转录入页 |
| F | 医生独立临床流程 | 签署/复核基础有；独立医生工作台与完整临床流程未做（驾驶舱亦标非目标） |
| G | 激活 Phase 2 | 手机对齐 / 短信 OTP —— 绑 SMS，暂缓 |

---

## 4. 产品取舍（不是欠债）

以下是刻意边界，**不要当成「漏做」重开**：

- 用药未完成 **不**自动开工作台催办单（P0 故意；看板可催随访/打卡跟进）
- 随访单 **须人工办结**（患者补打卡只关 `PLAN_NUDGE`）
- AI 只出草稿/预填，**人确认后**才 publish / 写库
- 消息中心是通知内核，不是医患 IM

---

## 5. 额度恢复后建议顺序

1. **W5** — C `/discover` 接业务工具（患者侧 AI 叙事补齐）  
2. **患者合并前端** — 后端已就绪，补 UI 即可收口「同人多档」  
3. **驾驶舱 P1+** — 气泡结构化动作卡、右栏草稿箱、简报/对话持久化加深  
4. 其余：排期可配、对话 OCR 占位、医生流程加深 —— 按客需再排  

外部触达通道：等账号到位单开，不插队。

---

## 6. 相关文档

| 文档 | 用途 |
|---|---|
| `收尾计划` | W1–W6 条目与做法（部分条目描述仍偏「待做」，以本文状态为准） |
| `智能驾驶舱-信息架构.md` | 驾驶舱 A/B/C/D 与 P0–P3 |
| `C端患者激活与账号绑定.md` | 激活 Phase 2、合并与绑定边界 |
| `依从性看板-产品与技术方案.md` / `工作台任务-技术方案.md` | 看板与任务闭环细节 |

---

## 7. 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v0.1 | 2026-09-09 | 首版复盘（无扩展名草稿） |
| v0.2 | 2026-09-09 | 改正式 md；按代码校正：W4/MFA/配额·flag·白标/医生基础已落地；合并=后端有前端无；整理暂缓与续作顺序 |

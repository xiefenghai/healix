<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.org.mapper.OrgInviteCodeMapper">

    <select id="findByCode" resultType="com.healix.core.org.domain.OrgInviteCode">
        SELECT pk_id, id, tenant_id, org_id, code, enabled, expire_at, created_by_staff_id,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM org_invite_code
        WHERE code = #{code} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO org_invite_code(
            id, tenant_id, org_id, code, enabled, expire_at, created_by_staff_id,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{tenantId}, #{orgId}, #{code}, #{enabled}, #{expireAt}, #{createdByStaffId},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="refresh">
        UPDATE org_invite_code
        SET code = #{code}, expire_at = #{expireAt}, enabled = 1, gmt_modified = #{gmtModified}
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.job.mapper.SysJobDefMapper">

    <sql id="cols">
        pk_id, id, job_code, display_name, cron_expr, timezone, enabled, params_json,
        next_fire_at, last_fire_at, last_status, last_message,
        is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO sys_job_def(
            id, job_code, display_name, cron_expr, timezone, enabled, params_json,
            next_fire_at, last_fire_at, last_status, last_message,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{jobCode}, #{displayName}, #{cronExpr}, #{timezone}, #{enabled}, #{paramsJson},
            #{nextFireAt}, #{lastFireAt}, #{lastStatus}, #{lastMessage},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="updateConfig">
        UPDATE sys_job_def
        SET display_name = #{displayName},
            cron_expr = #{cronExpr},
            timezone = #{timezone},
            enabled = #{enabled},
            params_json = #{paramsJson},
            next_fire_at = #{nextFireAt},
            gmt_modified = #{gmtModified}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="updateFireState">
        UPDATE sys_job_def
        SET next_fire_at = #{nextFireAt},
            last_fire_at = #{lastFireAt},
            last_status = #{lastStatus},
            last_message = #{lastMessage},
            gmt_modified = #{gmtModified}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <select id="findByJobCode" resultType="com.healix.core.job.domain.SysJobDef">
        SELECT <include refid="cols"/>
        FROM sys_job_def
        WHERE job_code = #{jobCode}
          AND is_deleted = 0
    </select>

    <select id="listAll" resultType="com.healix.core.job.domain.SysJobDef">
        SELECT <include refid="cols"/>
        FROM sys_job_def
        WHERE is_deleted = 0
        ORDER BY job_code
    </select>

    <select id="listDue" resultType="com.healix.core.job.domain.SysJobDef">
        SELECT <include refid="cols"/>
        FROM sys_job_def
        WHERE is_deleted = 0
          AND enabled = 1
          AND next_fire_at IS NOT NULL
          AND next_fire_at &lt;= #{now}
        ORDER BY next_fire_at
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.adherence.mapper.AdherenceDailySnapshotMapper">

    <sql id="cols">
        pk_id, id, tenant_id, org_id, care_team_id, snapshot_date,
        universe_count, follow_up_count, plan_incomplete_count, med_incomplete_count, streak_ge3_count,
        plan_due_sum, plan_done_sum, med_due_dose_sum, med_taken_dose_sum, plan_rate, med_rate,
        is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <insert id="upsert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO adherence_daily_snapshot(
            id, tenant_id, org_id, care_team_id, snapshot_date,
            universe_count, follow_up_count, plan_incomplete_count, med_incomplete_count, streak_ge3_count,
            plan_due_sum, plan_done_sum, med_due_dose_sum, med_taken_dose_sum, plan_rate, med_rate,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{tenantId}, #{orgId}, #{careTeamId}, #{snapshotDate},
            #{universeCount}, #{followUpCount}, #{planIncompleteCount}, #{medIncompleteCount}, #{streakGe3Count},
            #{planDueSum}, #{planDoneSum}, #{medDueDoseSum}, #{medTakenDoseSum}, #{planRate}, #{medRate},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
        ON DUPLICATE KEY UPDATE
            universe_count = VALUES(universe_count),
            follow_up_count = VALUES(follow_up_count),
            plan_incomplete_count = VALUES(plan_incomplete_count),
            med_incomplete_count = VALUES(med_incomplete_count),
            streak_ge3_count = VALUES(streak_ge3_count),
            plan_due_sum = VALUES(plan_due_sum),
            plan_done_sum = VALUES(plan_done_sum),
            med_due_dose_sum = VALUES(med_due_dose_sum),
            med_taken_dose_sum = VALUES(med_taken_dose_sum),
            plan_rate = VALUES(plan_rate),
            med_rate = VALUES(med_rate),
            gmt_modified = VALUES(gmt_modified)
    </insert>

    <select id="listRange" resultType="com.healix.core.adherence.domain.AdherenceDailySnapshot">
        SELECT <include refid="cols"/>
        FROM adherence_daily_snapshot
        WHERE org_id = #{orgId}
          AND care_team_id = #{careTeamId}
          AND snapshot_date BETWEEN #{from} AND #{to}
          AND is_deleted = 0
        ORDER BY snapshot_date ASC
    </select>
</mapper>

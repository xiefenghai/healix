<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.care.mapper.CareTeamMemberMapper">

    <sql id="cols">
        pk_id, id, tenant_id, org_id, team_id, member_type, staff_id, people_id,
        joined_at, is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <select id="findById" resultType="com.healix.core.care.domain.CareTeamMember">
        SELECT <include refid="cols"/>
        FROM care_team_member
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="listByTeam" resultType="com.healix.core.care.domain.CareTeamMember">
        SELECT <include refid="cols"/>
        FROM care_team_member
        WHERE team_id = #{teamId} AND is_deleted = 0
        ORDER BY id
    </select>

    <select id="findStaff" resultType="com.healix.core.care.domain.CareTeamMember">
        SELECT <include refid="cols"/>
        FROM care_team_member
        WHERE team_id = #{teamId} AND staff_id = #{staffId}
          AND member_type = 'STAFF' AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="findPeopleInOrg" resultType="com.healix.core.care.domain.CareTeamMember">
        SELECT <include refid="cols"/>
        FROM care_team_member
        WHERE org_id = #{orgId} AND people_id = #{peopleId}
          AND member_type = 'PATIENT' AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="countActiveByTeam" resultType="int">
        SELECT COUNT(1)
        FROM care_team_member
        WHERE team_id = #{teamId} AND is_deleted = 0
    </select>

    <select id="countPrimaryTeamsForStaff" resultType="int">
        SELECT COUNT(1)
        FROM care_team
        WHERE org_id = #{orgId}
          AND is_deleted = 0
          AND (primary_care_manager_staff_id = #{staffId} OR primary_doctor_staff_id = #{staffId})
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO care_team_member(
            id, tenant_id, org_id, team_id, member_type, staff_id, people_id,
            joined_at, is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{tenantId}, #{orgId}, #{teamId}, #{memberType}, #{staffId}, #{peopleId},
            #{joinedAt}, #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="softDelete">
        UPDATE care_team_member
        SET is_deleted = 1, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtModified}
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="softDeleteByTeam">
        UPDATE care_team_member
        SET is_deleted = 1, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtModified}
        WHERE team_id = #{teamId} AND is_deleted = 0
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.job.mapper.SysJobRunMapper">

    <sql id="cols">
        pk_id, id, job_def_id, job_code, trigger_type, status, started_at, finished_at,
        message, detail_json, instance_id, triggered_by,
        is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO sys_job_run(
            id, job_def_id, job_code, trigger_type, status, started_at, finished_at,
            message, detail_json, instance_id, triggered_by,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{jobDefId}, #{jobCode}, #{triggerType}, #{status}, #{startedAt}, #{finishedAt},
            #{message}, #{detailJson}, #{instanceId}, #{triggeredBy},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="updateFinish">
        UPDATE sys_job_run
        SET status = #{status},
            finished_at = #{finishedAt},
            message = #{message},
            detail_json = #{detailJson},
            gmt_modified = #{gmtModified}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <select id="listByJobCode" resultType="com.healix.core.job.domain.SysJobRun">
        SELECT <include refid="cols"/>
        FROM sys_job_run
        WHERE job_code = #{jobCode}
          AND is_deleted = 0
        ORDER BY started_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByJobCode" resultType="long">
        SELECT COUNT(1)
        FROM sys_job_run
        WHERE job_code = #{jobCode}
          AND is_deleted = 0
    </select>
</mapper>

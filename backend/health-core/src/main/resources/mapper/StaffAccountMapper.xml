<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.identity.mapper.StaffAccountMapper">

    <select id="findByUsername" resultType="com.healix.core.identity.domain.StaffAccount">
        SELECT pk_id, id, username, password_hash, status,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_account
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <select id="findById" resultType="com.healix.core.identity.domain.StaffAccount">
        SELECT pk_id, id, username, password_hash, status,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_account
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO staff_account(
            id, username, password_hash, status,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{username}, #{passwordHash}, #{status},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="updateStatusAndPassword">
        UPDATE staff_account
        SET status = #{status},
            <if test="passwordHash != null and passwordHash != ''">
                password_hash = #{passwordHash},
            </if>
            gmt_modified = #{gmtModified}
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>

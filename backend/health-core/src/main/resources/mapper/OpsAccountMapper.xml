<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.identity.mapper.OpsAccountMapper">

    <select id="findByUsername" resultType="com.healix.core.identity.domain.OpsAccount">
        SELECT pk_id, id, username, password_hash, display_name, role_code, status,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM ops_account
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(1) FROM ops_account WHERE is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO ops_account(
            id, username, password_hash, display_name, role_code, status,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{username}, #{passwordHash}, #{displayName}, #{roleCode}, #{status},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>
</mapper>

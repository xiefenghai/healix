<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.patient.mapper.PatientOrgMembershipMapper">

    <select id="findByOrgAndPeople" resultType="com.healix.core.patient.domain.PatientOrgMembership">
        SELECT pk_id, id, tenant_id, org_id, people_id, status, joined_at, left_at,
               invited_by_staff_id, is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM people_org_membership
        WHERE org_id = #{orgId} AND people_id = #{peopleId} AND is_deleted = 0
    </select>

    <select id="listActiveByPeople" resultType="com.healix.core.patient.domain.PatientOrgMembership">
        SELECT pk_id, id, tenant_id, org_id, people_id, status, joined_at, left_at,
               invited_by_staff_id, is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM people_org_membership
        WHERE people_id = #{peopleId}
          AND status = 'ACTIVE'
          AND left_at IS NULL
          AND is_deleted = 0
        ORDER BY id
    </select>

    <select id="listActiveByOrg" resultType="com.healix.core.patient.domain.PatientOrgMembership">
        SELECT pk_id, id, tenant_id, org_id, people_id, status, joined_at, left_at,
               invited_by_staff_id, is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM people_org_membership
        WHERE org_id = #{orgId}
          AND status = 'ACTIVE'
          AND left_at IS NULL
          AND is_deleted = 0
        ORDER BY id
    </select>

    <select id="countActiveInTenant" resultType="int">
        SELECT COUNT(1)
        FROM people_org_membership
        WHERE people_id = #{peopleId}
          AND tenant_id = #{tenantId}
          AND status = 'ACTIVE'
          AND left_at IS NULL
          AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO people_org_membership(
            id, tenant_id, org_id, people_id, status, joined_at, left_at,
            invited_by_staff_id, is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{tenantId}, #{orgId}, #{peopleId}, #{status}, #{joinedAt}, #{leftAt},
            #{invitedByStaffId}, #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="markLeft">
        UPDATE people_org_membership
        SET status = 'LEFT', left_at = #{leftAt}, gmt_modified = #{leftAt}
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.patientcard.mapper.AccountPatientMapper">

    <sql id="cols">
        pk_id, id, tenant_id, account_id, people_id, display_name, relation,
        identity_type, identity_value_hash, identity_value_mask, mobile,
        is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <select id="findById" resultType="com.healix.core.patientcard.domain.AccountPatient">
        SELECT <include refid="cols"/>
        FROM account_patient
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="listByAccount" resultType="com.healix.core.patientcard.domain.AccountPatient">
        SELECT <include refid="cols"/>
        FROM account_patient
        WHERE account_id = #{accountId} AND is_deleted = 0
        ORDER BY gmt_created ASC
    </select>

    <select id="listByPeople" resultType="com.healix.core.patientcard.domain.AccountPatient">
        SELECT <include refid="cols"/>
        FROM account_patient
        WHERE people_id = #{peopleId} AND is_deleted = 0
        ORDER BY gmt_created ASC
    </select>

    <select id="listLinkedPeopleIds" resultType="java.lang.String">
        SELECT DISTINCT people_id
        FROM account_patient
        WHERE is_deleted = 0
          AND people_id IN
        <foreach collection="peopleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="countByAccount" resultType="int">
        SELECT COUNT(1) FROM account_patient
        WHERE account_id = #{accountId} AND is_deleted = 0
    </select>

    <select id="countByPeople" resultType="int">
        SELECT COUNT(1) FROM account_patient
        WHERE people_id = #{peopleId} AND is_deleted = 0
    </select>

    <select id="findByAccountAndPeople" resultType="com.healix.core.patientcard.domain.AccountPatient">
        SELECT <include refid="cols"/>
        FROM account_patient
        WHERE account_id = #{accountId} AND people_id = #{peopleId} AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="findByAccountAndIdentityHash" resultType="com.healix.core.patientcard.domain.AccountPatient">
        SELECT <include refid="cols"/>
        FROM account_patient
        WHERE account_id = #{accountId}
          AND identity_type = #{identityType}
          AND identity_value_hash = #{identityValueHash}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO account_patient(
            id, tenant_id, account_id, people_id, display_name, relation,
            identity_type, identity_value_hash, identity_value_mask, mobile,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{tenantId}, #{accountId}, #{peopleId}, #{displayName}, #{relation},
            #{identityType}, #{identityValueHash}, #{identityValueMask}, #{mobile},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="softDelete">
        UPDATE account_patient
        SET is_deleted = 1, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtDeleted}
        WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>

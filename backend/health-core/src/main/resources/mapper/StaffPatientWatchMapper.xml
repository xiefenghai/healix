<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.workspace.mapper.StaffPatientWatchMapper">

    <select id="findActive" resultType="com.healix.core.workspace.domain.StaffPatientWatch">
        SELECT pk_id, id, tenant_id, org_id, staff_id, people_id,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_patient_watch
        WHERE staff_id = #{staffId}
          AND org_id = #{orgId}
          AND people_id = #{peopleId}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="listPeopleIdsByStaffOrg" resultType="java.lang.String">
        SELECT people_id
        FROM staff_patient_watch
        WHERE staff_id = #{staffId}
          AND org_id = #{orgId}
          AND is_deleted = 0
    </select>

    <select id="countByStaffOrg" resultType="int">
        SELECT COUNT(1)
        FROM staff_patient_watch
        WHERE staff_id = #{staffId}
          AND org_id = #{orgId}
          AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO staff_patient_watch(
            id, tenant_id, org_id, staff_id, people_id,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{tenantId}, #{orgId}, #{staffId}, #{peopleId},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="softDelete">
        UPDATE staff_patient_watch
        SET is_deleted = 1, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtModified}
        WHERE staff_id = #{staffId}
          AND org_id = #{orgId}
          AND people_id = #{peopleId}
          AND is_deleted = 0
    </update>
</mapper>

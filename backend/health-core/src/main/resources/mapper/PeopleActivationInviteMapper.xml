<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.patientcard.mapper.PeopleActivationInviteMapper">

    <sql id="cols">
        pk_id, id, tenant_id, org_id, people_id, code, enabled, expire_at,
        used_at, used_by_account_id, created_by_staff_id,
        is_deleted, gmt_created, gmt_modified, gmt_deleted
    </sql>

    <select id="findById" resultType="com.healix.core.patientcard.domain.PeopleActivationInvite">
        SELECT <include refid="cols"/>
        FROM people_activation_invite
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="findByCode" resultType="com.healix.core.patientcard.domain.PeopleActivationInvite">
        SELECT <include refid="cols"/>
        FROM people_activation_invite
        WHERE code = #{code} AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="listByPeople" resultType="com.healix.core.patientcard.domain.PeopleActivationInvite">
        SELECT <include refid="cols"/>
        FROM people_activation_invite
        WHERE people_id = #{peopleId} AND is_deleted = 0
        ORDER BY gmt_created DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO people_activation_invite(
            id, tenant_id, org_id, people_id, code, enabled, expire_at,
            used_at, used_by_account_id, created_by_staff_id,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{tenantId}, #{orgId}, #{peopleId}, #{code}, #{enabled}, #{expireAt},
            #{usedAt}, #{usedByAccountId}, #{createdByStaffId},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <update id="markUsed">
        UPDATE people_activation_invite
        SET used_at = #{usedAt},
            used_by_account_id = #{usedByAccountId},
            enabled = 0,
            gmt_modified = #{gmtModified}
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="disableUnusedByPeople">
        UPDATE people_activation_invite
        SET enabled = 0, gmt_modified = NOW()
        WHERE people_id = #{peopleId}
          AND is_deleted = 0
          AND used_at IS NULL
          AND enabled = 1
    </update>

    <update id="softDelete">
        UPDATE people_activation_invite
        SET is_deleted = 1, enabled = 0, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtDeleted}
        WHERE id = #{id} AND is_deleted = 0 AND used_at IS NULL
    </update>
</mapper>

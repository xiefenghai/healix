<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.identity.mapper.StaffRoleBindingMapper">

    <select id="listByStaff" resultType="com.healix.core.identity.domain.StaffRoleBinding">
        SELECT pk_id, id, tenant_id, staff_id, role_code, org_id,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_role_binding
        WHERE staff_id = #{staffId} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO staff_role_binding(
            id, tenant_id, staff_id, role_code, org_id,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{tenantId}, #{staffId}, #{roleCode}, #{orgId},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>

    <select id="listByStaffIds" resultType="com.healix.core.identity.domain.StaffRoleBinding">
        SELECT pk_id, id, tenant_id, staff_id, role_code, org_id,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_role_binding
        WHERE is_deleted = 0
          AND staff_id IN
        <foreach collection="staffIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findTenantAdminByStaff" resultType="com.healix.core.identity.domain.StaffRoleBinding">
        SELECT pk_id, id, tenant_id, staff_id, role_code, org_id,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM staff_role_binding
        WHERE staff_id = #{staffId}
          AND role_code = 'TENANT_ADMIN'
          AND org_id IS NULL
          AND is_deleted = 0
        LIMIT 1
    </select>

    <update id="softDeleteAssignableByStaff">
        UPDATE staff_role_binding
        SET is_deleted = 1, gmt_deleted = #{gmtDeleted}, gmt_modified = #{gmtModified}
        WHERE staff_id = #{staffId}
          AND is_deleted = 0
          AND role_code IN ('CARE_MANAGER', 'DOCTOR', 'ORG_ADMIN', 'TENANT_OPERATOR')
    </update>
</mapper>

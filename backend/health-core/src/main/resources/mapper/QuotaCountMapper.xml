<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.govern.mapper.QuotaCountMapper">

    <select id="countPatients" resultType="long">
        SELECT COUNT(1) FROM people_profile WHERE tenant_id = #{tenantId} AND is_deleted = 0
    </select>

    <select id="countStaff" resultType="long">
        SELECT COUNT(1) FROM staff_profile WHERE tenant_id = #{tenantId} AND is_deleted = 0
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.core.people.mapper.PeopleAccountMapper">

    <select id="findByTenantAndUsername" resultType="com.healix.core.people.domain.PeopleAccount">
        SELECT pk_id, id, tenant_id, username, password_hash, status,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM people_account
        WHERE tenant_id = #{tenantId} AND username = #{username} AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="findById" resultType="com.healix.core.people.domain.PeopleAccount">
        SELECT pk_id, id, tenant_id, username, password_hash, status,
               is_deleted, gmt_created, gmt_modified, gmt_deleted
        FROM people_account
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO people_account(
            id, tenant_id, username, password_hash, status,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        )
        VALUES (
            #{id}, #{tenantId}, #{username}, #{passwordHash}, #{status},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>
</mapper>

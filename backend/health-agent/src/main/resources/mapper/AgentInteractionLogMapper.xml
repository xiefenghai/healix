<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healix.agent.log.AgentInteractionLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO agent_interaction_log(
            id, session_id, agent_type, tenant_id, people_id, staff_id, intent,
            user_message, prompt_snapshot, tool_calls_json, draft_reply, final_reply,
            prompt_tokens, completion_tokens,
            is_deleted, gmt_created, gmt_modified, gmt_deleted
        ) VALUES (
            #{id}, #{sessionId}, #{agentType}, #{tenantId}, #{peopleId}, #{staffId}, #{intent},
            #{userMessage}, #{promptSnapshot}, #{toolCallsJson}, #{draftReply}, #{finalReply},
            #{promptTokens}, #{completionTokens},
            #{isDeleted}, #{gmtCreated}, #{gmtModified}, #{gmtDeleted}
        )
    </insert>
</mapper>
